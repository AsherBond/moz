<% @comments.each do |comment| %>
	<blockquote>
		<p><%= comment.content %></p>
		<small><%= link_to comment.user.username, comment.user %> on <%= comment.created_at.strftime('%b %d, %Y at %H:%M') %></small>
	</blockquote>
<% end %>
<% if @comments.empty? %>
	<div class="alert alert-warning">There are no comments. Be the first! <a class="close" data-dismiss="alert" href="#">×</a></div>
<% end %>